# Policy Index
- Rooms map lives in `policy/rooms/`.
- Retention + defaults in `policy/retention/`.
- Gatekeeping + access in `policy/gates/`.
- Each policy change must include a Change Note + rollback.
